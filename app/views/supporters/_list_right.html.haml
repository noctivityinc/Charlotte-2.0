Those Showing Support...
#with_logos
  - Supporter.with_logos.each do |supporter|
    .supporter
      - if supporter.website
        = link_to image_tag(supporter.logo.url(:medium), :border => "0"), supporter.website, :target => "_blank" 
      - else
        = image_tag(supporter.logo.url(:medium), :border => "0")