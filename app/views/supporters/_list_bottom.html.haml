The Charlotte 2.0 Initiative Is Supported By The Following Companies
#with_logos
  - Supporter.with_logos.each do |supporter|
    .supporter
      - if supporter.website
        = link_to image_tag(supporter.logo.url(:medium), :border => "0"), supporter.website, :target => "_blank" 
      - else
        = image_tag(supporter.logo.url(:medium), :border => "0")
#without_logos
  - Supporter.without_logos.each do |supporter|          
    .supporter
      - if supporter.website
        = link_to supporter.company, supporter.website, :target => "_blank" 
      - else
        = supporter.company_