#posts
  #index
    - title "Posts"

    %table
      %tr
        %th Title
        %th Body
        %th(colspan="3")
      - for post in @posts
        %tr
          %td= h post.title
          %td= h post.body
          %td= link_to 'Show', [:admin, post]
          %td= link_to 'Edit', edit_admin_post_path(post)
          %td= link_to 'Destroy', [:admin, post], :confirm => 'Are you sure?', :method => :delete

    %p= link_to "+ New Post", new_admin_post_path
